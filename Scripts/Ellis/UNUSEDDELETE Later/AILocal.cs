using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AILocal : MonoBehaviour
{
    //public MoveTo moveto;

    //// Start is called before the first frame update
    //public void Start()
    //{
    //    MoveTo mmoveTo = moveto.GetComponent<MoveTo>();
        
    //}

    //// Update is called once per frame
    //public void FixedUpdate()
    //{
    //    if (Mathf.Approximately(this.transform.position.x, moveto.goal.position.x) && Mathf.Approximately(this.transform.position.y, moveto.goal.position.y) && moveto.called == false)
    //    {
    //        moveto.called = true;
    //        StartCoroutine(moveto.waitAtLocation());
    //        Debug.Log("Called transforms are same");

    //    }
    //}
}
